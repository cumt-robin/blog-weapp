<view class="card-article">
  <text class="card-title">{{item.article_name}}</text>
  <image
    class='img-poster'
    wx:if="{{item.poster}}"
    lazy-load
    mode="aspectFill"
    src="{{item.poster}}">
  </image>
</view>